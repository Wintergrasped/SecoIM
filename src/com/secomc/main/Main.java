package com.secomc.main;

import org.bukkit.plugin.java.JavaPlugin;

import com.secomc.listeners.ExampleItemListener;


public class Main extends JavaPlugin {

	
	public void onEnable() {
		//Example of how to start your listener just Dont comment out yours this example is just commented out cause the example code is from the Econ
		//                                                 Replace 'ExampleItmeListener()' with the name of the class you created with your listener in it.
		//getServer().getPluginManager().registerEvents(new ExampleItemListener(), this);
	}
	
	
}
